{
  "name": "proof-theme-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Unofficial Proof Light themes for VS Code and IntelliJ Platform IDEs.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "package:vscode": "cd vscode && npx @vscode/vsce package --no-dependencies --allow-missing-repository --out ../dist/proof-light-vscode-0.1.0.vsix",
    "package:intellij": "bash scripts/package-intellij.sh",
    "package": "npm run validate && npm run package:vscode && npm run package:intellij"
  }
}
