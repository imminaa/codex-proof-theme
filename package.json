{
  "name": "codex-theme-pack-workspace",
  "version": "0.2.0",
  "private": true,
  "description": "Unofficial Codex appearance theme pack for VS Code and IntelliJ Platform IDEs.",
  "scripts": {
    "generate": "node scripts/generate-theme-pack.mjs",
    "validate": "node scripts/validate.mjs",
    "package:vscode": "node scripts/package-vscode.mjs",
    "package:intellij": "node scripts/package-intellij.mjs",
    "package": "npm run generate && npm run validate && npm run package:vscode && npm run package:intellij"
  }
}
